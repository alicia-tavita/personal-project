import { useState } from 'react'
//stretch import useparams

export default function Completed() {
  // if (isError) {
  // }

  // if (isLoading) {
  // }

  // if (data)
  return (
    <div className="todoContainer">
      <h1>Completed Todos</h1>
      <ul>
        {/* map in here over data */}
        {/* Button to add to completed? */}
        {/* Input type = checkbox */}
      </ul>
      <Completed />
    </div>
  )
}
